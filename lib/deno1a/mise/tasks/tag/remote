#!/usr/bin/env bash
#MISE description="Show version tags on the remote (bare semver, no v-prefix)"
set -euo pipefail
echo "Remote tags:"
git ls-remote --tags origin 'refs/tags/[0-9]*' 2>/dev/null | \
  sed 's|.*refs/tags/||' | grep -v '\^{}' | grep -E '^[0-9]+\.[0-9]+\.[0-9]+' | sort -rV
